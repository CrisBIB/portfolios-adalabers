"use strict";

/* const buttonElement = document.querySelector(".js-buttonPrev");

const hadleButton = (ev) => {
  ev.preventDefault();
  const url = "";
  const data = getUserData();
  fetch(url, {
    method: "POST",
    body: JSON.stringify(data),
    headers: {
      "Content-Type": "application/json",
    },
  })
    .then((response) => response.json())
    .then((data) => {
      console.log("Server response:", data);
    });
};

buttonElement.addEventListener("click", hadleButton);
 */
